name: Check YouTube Videos
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:  # Ручной запуск

jobs:
  check-and-publish:
    runs-on: ubuntu-latest
    # Force cache refresh 2026-04-11  ← Добавьте эту строку!
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
